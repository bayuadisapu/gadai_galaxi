import 'package:flutter/material.dart';

class AppColors {
  // ── Primary Navy Palette ──
  static const Color navyDeep    = Color(0xFF0A1628);
  static const Color navyMid     = Color(0xFF102A4C);
  static const Color navyAccent  = Color(0xFF1E4ED8);
  static const Color royalBlue   = Color(0xFF2563EB);
  static const Color skyBlue     = Color(0xFF3B82F6);
  static const Color iceBlue     = Color(0xFFEFF6FF);

  // ── Accent & Highlight ──
  static const Color gold        = Color(0xFFF59E0B);
  static const Color emerald     = Color(0xFF10B981);
  static const Color coral       = Color(0xFFEF4444);
  static const Color violet      = Color(0xFF7C3AED);

  // ── Surfaces ──
  static const Color surface     = Color(0xFFF8FAFC);
  static const Color white       = Color(0xFFFFFFFF);
  static const Color cardBorder  = Color(0xFFE2E8F0);

  // ── Text ──
  static const Color textDark    = Color(0xFF0F172A);
  static const Color textMuted   = Color(0xFF64748B);
  static const Color textSecond  = Color(0xFF475569);

  // ── Legacy aliases (backward compat) ──
  static const Color primary              = royalBlue;
  static const Color background           = navyDeep;
  static const Color cardBackground       = white;
  static const Color textInputLabel       = Color(0xFF1E293B);
  static const Color textInputHint        = Color(0xFF94A3B8);
  static const Color inputBackground      = Color(0xFFF1F5F9);
  static const Color inputFocusedBorder   = royalBlue;
}

